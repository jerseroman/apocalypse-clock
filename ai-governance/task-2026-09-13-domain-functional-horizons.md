# Task

Replace misleading domain priority-mass horizons with propagated functional-cascade horizons and make regime first passage responsive to the dataset's sampled latent-pressure growth.

## Change class

MODEL.

Exact owner instruction authorizing this class: "popravi tako da bo pravilno! potem objavi na github in na spletni strani!"

## Files touched

- `src/app.js`: `sampleRegimeHorizon`, `deterministicRegimeHorizon`, `computeThreatHorizon`, `configuredFunctionalCascadeNodes`, `simulateFunctionalCascade`, `computeDomainFunctionalCrossing`, `recordMonteCarloSample`, `summarizeMonteCarloAccumulator`, domain-card rendering, execution snapshot and numerical fingerprint.
- `src/cascade-model.js`: add pure `firstCrossingFromActivationYears` reporting-basket reconstruction.
- `index.html`, `404.html`: domain-method explanation and release identity only; bundled numerical data remain unchanged.
- `tests/functional-cascade.spec.js`, `tests/units.spec.js`, `tests/functional-integration.spec.js`, `tests/headline-determinism.spec.js`: monotonicity, domain reconstruction, censoring, export and golden contracts.
- `package.json`, `package-lock.json`, `CITATION.cff`, `.zenodo.json`, `README.md`, `CHANGELOG.md`, `docs/*`, `ai-governance/*`: model 1.2.9 identity, rationale, limitations and delivery artifacts.

## Files explicitly NOT touched

- No `mu`, `lo`, `hi`, threshold or growth-rate values.
- No threat taxonomy, process-type assignment, dependency topology, edge weight, lag, functional criticality tier, service membership or overlap group.
- No time axis, global or per-threat threshold policy/bounds, domain/profile weights, PRNG seed/algorithm, quantile interpolation or 2101 sentinel meaning.
- No vendor libraries, license, notice, statement or historical validation artifacts.

## Affected outputs

- DOM nodes: `#aggregateRow` domain cards and domain heatmap source data.
- Export fields: adds domain functional summaries and their meaning under `functionalResults`; existing fields remain.
- Headline-related numbers: yes. Candidate baseline Dynamic Cascade changes from P10/P50/P90 2033/2036/2043 to 2033/2036/2042.
- Determinism under default seed: changed intentionally; same-configuration repeatability must remain exact.

## Numerical risk

- Changes regime horizon timing from an uncalibrated growth-blind geometric clock to sampled latent-pressure first passage.
- Changes domain P10/P50/P90 from a 40% sampled-priority-mass aggregate to fixed-criticality, overlap-aware, essential-service functional first crossing reconstructed from full-system propagation.
- Could shift standalone, compensatory, graph, cascade, domain and sensitivity outputs. Ordered quantiles, censoring, monotonicity, no-crossing semantics and reproducibility require regression checks.
- The change is consistent with the retained direct-growth interpretation because it uses already calibrated engine-unit `g` without reconversion or parameter retuning. It does not establish predictive calibration.

## Validation strategy

- Run all items 1-10 in `validation-checklist.md`, including a full Playwright suite and manual desktop/mobile geometry inspection.
- Include headline-determinism step 8 with recorded before/after and exact repeated runs.
- Record the new 1.2.9 golden only after an independent capture from the fixed baseline configuration.
- Verify exports contain the domain method, no raw 2101 is displayed, all domain quantiles are ordered, and censored medians are labeled unidentified.

## Rollback plan

- Revert the resulting Git commit, or restore the verified pre-change backup at `C:/Users/error/Desktop/Apocalypse Clock backups/pre-domain-functional-fix-20260913` whose baseline commit is `c39662e630c755706cb384663a9c9cf322b970ce`.

## Status

recorded
